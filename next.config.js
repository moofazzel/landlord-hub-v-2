/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ["lh3.googleusercontent.com", "cdn.pixabay.com"], //Domain of image host
  },
};

module.exports = nextConfig;
